# ============================================================================
# AUTO-GENERATED BY NIX
# ============================================================================
# This file points to dependencies in the /nix/store.
# If you edit a dependency, you must rebuild the flake for this file to update.

[package]
authors = ["Sven Cattell"]
edition = "2024"
name = "serial_client"
version = "0.1.0"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
capability = ["dep:tokio", "dep:tokio-serial"]
default = []
module = []

[dependencies]
pyroduct = { workspace = true }
tokio = { features = ["full"], optional = true, version = "1.49.0" }
tokio-serial = { optional = true, version = "5.4.1" }
